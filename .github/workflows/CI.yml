# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: CI

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

  workflow_dispatch:

permissions:
  contents: read

jobs:
  api-lint:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/cmnemoi/emush/php:latest
      credentials:
        username: cmnemoi
        password: ${{  secrets.DOCKER_CONTAINER_REGISTRY_TOKEN }}
    steps:
      - name: check
        run: echo "Hello, World! from PHP container!"
