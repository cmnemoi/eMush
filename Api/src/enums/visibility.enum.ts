export enum VisibilityEnum {
    PUBLIC = 'public',
    PRIVATE = 'private',
    COVERT = 'covert', // revealed by camera
    SECRET = 'secret', // revealed by camera or someone
    MUSH = 'mush', // logs in mush channel
}
